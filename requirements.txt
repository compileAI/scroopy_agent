# Core Python packages
requests>=2.32.0
python-dotenv>=1.0.0
beautifulsoup4>=4.12.0

# Data validation and models
pydantic>=2.0.0

# Database integration
supabase>=2.3.0

# Date parsing
dateparser>=1.2.0
python-dateutil>=2.8.0

# Async file operations (for Stagehand caching)
aiofiles>=23.0.0

# Stagehand web automation (optional - only needed for stagehand_main.py)
# Uncomment if using Stagehand strategy:
# stagehand>=0.1.0

# HTTP retries and session management
urllib3>=2.0.0

# LXML for faster HTML parsing (optional but recommended)
lxml>=4.9.0

# Scraping dependencies (ported from compile)
crawl4ai>=0.4.0
praw>=7.0.0
asyncpraw>=7.0.0
feedparser>=6.0.0
